from django.urls import path, re_path

from . import views

urlpatterns = [
    # path('actors/', views.ActorListView.as_view()),
    # re_path('^actors/(?P<pk>\d+)/$', views.ActorDetailView.as_view()),
]
